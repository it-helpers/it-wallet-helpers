{
    "$id": "https://example.com/wte_proof_header.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "WTE proof header",
    "description": "WTE proof header",
    "type": "object",
    "required": [
        "typ",
        "alg",
        "kid"
    ],
    "properties": {
        "typ": {
            "type": "string",
            "description": "A field with the value jwt-client-attestation-pop",
            "example": "jwt-client-attestation-pop"
        },
        "alg": {
            "type": "string",
            "description": "An identifier for a JWS asymmetric digital signature algorithm. It MUST NOT be none or an identifier for a symmetric algorithm",
            "example": "ES256"
        },
        "kid": {
            "type": "string",
            "description": "An identifier that refers to a specific public key of the private key used to sign the WTE proof. This is used, for instance, to choose among a set of keys within a JWK Set",
            "example": "$base64url-encoded-thumprint-of-the-jwk-in-the-cnf-wallet-attestation$"
        }
    }
}